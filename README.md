# git

### comandos básicos

#### lista as branchs
git branch -v -a

#### lista remotes
git remote -v

#### add remote 
git remote add <nome> <url>

#### checkout
git checkout -b <branch>

#### deleta local
git branch --delete <branch>